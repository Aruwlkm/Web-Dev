import { Routes } from '@angular/router';
import {EventsListComponent } from './events-list.component/events-list.component';

export const routes: Routes = [
    {path:'',component: EventsListComponent}
];
